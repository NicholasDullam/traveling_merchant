import React from 'react'

const CheckoutInterface = (props) => {
    return (
        <div style={{ backgroundColor: 'black', position: 'absolute', left: '50%', width: '50%', minHeight: '100%', transform: 'translateX(-50%)' }}>
            
        </div>
    )
}

export default CheckoutInterface